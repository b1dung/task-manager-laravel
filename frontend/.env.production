# Loaded automatically by Vite for production builds (`vite build`).
# Realtime on production (task.mintoku.vn) uses Pusher cloud.
# These MUST match backend .env (PUSHER_APP_KEY / PUSHER_APP_CLUSTER).
VITE_API_URL=/api/v1
VITE_BROADCASTER=pusher
VITE_PUSHER_APP_KEY=ee583f9a9e7bdf6211e1
VITE_PUSHER_CLUSTER=ap1
