; Task attachment uploads up to 250 MB (see AttachmentController::MAX_KB).
; post_max_size must exceed upload_max_filesize to allow for multipart overhead.
upload_max_filesize = 250M
post_max_size = 256M
memory_limit = 512M
max_execution_time = 300
max_input_time = 300
